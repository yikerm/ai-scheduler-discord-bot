# AI Scheduler Bot 使用手冊

主要介面是 Discord Slash Commands 與 `@Bot` 自然語言。舊的 `!` 前綴指令沒有啟用，不應再使用。

## 日常使用

### 一般待辦

`@Bot` 可用自然語言判斷你想新增哪一類事項，例如：

```text
@Bot 明天下午六點前完成報告，預計 45 分鐘
@Bot 這星期六整理研究資料，大約一個半小時，可以分段完成
```

Bob 只使用自然語言判斷這是一般待辦及擷取任務名稱，接著開啟與 `/add` 相同的選單。原句中的日期、時間、時長與分割資訊不會直接寫入；必須在選單中重新選擇並按「建立任務」。因此即使 Gemini 誤判日期，也不會把任務排到錯誤日期。

### 固定行程

```text
@Bot 下星期四下午兩點安排 meeting，一個小時
@Bot 8 月 10 日上午 9 點到 10 點看醫生
```

Bob 判斷為固定行程後會開啟與 `/fixed` 相同的選單。原句中的日期、時間與時長只協助分類，不會預填或直接寫入；以你在選單中選擇的內容為準。

若直接使用舊自然語言解析，日期語意仍遵循以下規則；但新入口最終一律以選單為準：

- 「下星期四／下週四／下禮拜四」是下一個曆週的星期四。
- 單獨「星期四」是最近一次尚未錯過的星期四。
- 今天就是星期四且指定時間尚未經過時使用今天；時間已過則使用下週四。
- 所有相對日期以 `Asia/Taipei` 為基準。
- Gemini 或本地換算不一致時也不會直接建立。

### 重複任務

```text
@Bot 每天閱讀一個小時
@Bot 從 9 月 1 日開始，星期一到星期四每天語言練習 30 分鐘，持續到 12 月 31 日
@Bot 星期二到星期六晚上八點運動 30 分鐘，持續 30 天
@Bot 每星期天整理下週計畫 45 分鐘
```

這些語句只用來辨識「重複任務」與系列名稱，之後會開啟 `/repeat` 的兩頁選單。星期、固定／彈性時間、時長、開始日、分割及到期方式都必須在選單重新確認。

- 沒有指定開始時間：建立彈性重複任務，由排程器在當日作息內挑選空檔。
- 指定開始時間：建立固定時間重複行程。固定時段與 Calendar 衝突時會告知未建立的數量。
- 沒有指定截止日或持續天數：先建立 14 個日曆日；本期最後一天結束後，隔日約 00:10 私訊詢問「延長 14 天」或「結束系列」。
- 指定「持續到某日」：每天自動維持未來最多 14 天的行程，一直補建到截止日，截止後不再詢問延長。
- 指定「持續 N 天／接下來 N 週」：從實際開始日計算，到期直接結束，不詢問延長。
- 可用「從某日開始」指定系列開始日；若未指定，彈性任務通常從明天開始，今日尚未經過的固定時段可從今天開始。
- 「星期一到星期五」只代表發生星期，不會被解讀成系列截止日期。
- 重複與固定行程只在完成選單並按建立後寫入；刪除需按紅色確認按鈕，改期則交由 Google Calendar。

### 查詢任務與規劃

```text
@Bot 請列出所有尚未完成的任務
@Bot 查詢 8 月 5 日的語言練習
@Bot 顯示未來七天規劃
```

也可以使用：

```text
/tasks
/tasks query: 語言練習
/plan
```

未完成編號每次從 1 重新連續排列。狀態可能包含：

- `pending`：等待排程。
- `等待規劃`：期限仍在七天範圍之外。
- `無法排入`：已嘗試但空檔不足。
- `scheduled`：已排入 Calendar。
- `feedback_requested`：已結束，等待回饋。

### 改期與刪除

`@Bot 刪除任務` 會直接開啟與 `/delete` 相同的私人選單，不再依賴 Gemini 猜測名稱或未完成編號。若要求改期，Bob 不會修改資料，而會引導你直接在 Google Calendar 移動行程；下一個整點或半點同步後會反映到 `/tasks` 與 `/plan`。

也可以使用 `/delete` 開啟私人刪除選單。選單只列出 `pending` 與 `scheduled` 任務，不列出已結束等待評分、已完成或已有評分歷史的任務。一般及分割任務必須再次按「確認刪除」；分割任務會連同所有分段 Calendar 行程一起移除。

若選取的目標屬於重複系列，Bob 會提供「只刪除此行程／刪除整個系列／取消」三個選項。刪除整個系列會停止後續產生並移除尚未完成的未來行程，但已完成、未完成及評分歷史都會保留。

## 動態作息

作息分成四組：開學平日、開學六日、未開學平日、未開學六日。

```text
@Bot 開學時星期一到星期五早上七點到晚上十一點，星期六日早上九點到凌晨十二點
@Bot 沒開學時星期一到星期五早上十點到凌晨一點，星期六日中午十二點到凌晨兩點
@Bot 從九月一日開始使用開學作息
@Bot 現在切換成未開學模式
@Bot 明天可以排到晚上十一點半
@Bot 查看目前作息
```

修改作息會先確認；查詢作息直接回覆。單日例外只影響指定日期。Google Calendar 中的睡眠與其他忙碌行程永遠優先，ML 不能越過作息邊界。

## 即時排程與重排

- 新增一般待辦或彈性重複任務後，立即把「新任務＋未來 7 天尚未開始、可移動的彈性任務」交給 OR-Tools 一起最佳化。
- 最佳化會同時比較連續完成與允許的分段方案，不再等連續時段全部失敗才嘗試分段。
- 目標依序考量能否完成、一般／重要／緊急優先度、截止期限、ML 預期效率與精神、維持原排程的穩定性，以及減少過度切碎。
- 如果只需排入新任務且不動既有行程，會直接寫入 Calendar。
- 只要需要移動或重新分割任何既有任務，Bob 會先顯示 7 天異動摘要；按「確認重排」後才更新 Calendar。
- 系統會優先保留原時間，單純 ML 時段偏好不足以觸發搬動；單次提案最多移動 3 筆既有任務。
- 按「保持原排程」時，既有 Calendar 完全不變，新任務維持 `pending`。
- 固定、鎖定、已開始、已完成、非 Bot 行程，以及手動在 Calendar 移動後被鎖定的任務都不會移動。
- 每晚 23:59 也會執行相同的 7 天全局最佳化；若需要改動既有行程，同樣先私訊等待確認。

長任務是否允許分段及最短分段時間，都以 `/add` 或 `/repeat` 選單為準，可選擇 15、30、45、60 分鐘或自訂分鐘數。分段名稱會顯示 `(1/N)`，所有分段結束後只詢問一次回饋。
## 排程失敗通知

- 今天截止且即時重排失敗：立即回覆。
- 未來七天內新增且已確定無法排入：立即回覆。
- 每晚 23:59 正式排程失敗：Discord 私訊。
- 如果無法傳送私訊，會回到建立該任務的原頻道並標註你。
- 截止前 24 小時仍未排入：額外高優先提醒。
- 相同任務與相同原因 24 小時內不重複通知。

通知會包含所需分鐘數、截止時間、最長連續空檔與失敗原因。

## Google Calendar 優先規則

- 手動新增的 Calendar 行程會被視為忙碌時段。
- 手動移動 Bot 行程後，半小時同步會更新 SQLite，並將該任務鎖定。
- 不提供 `/reschedule`；請直接在 Google Calendar 移動行程，最長約 30 分鐘後反映至 `/tasks` 與 `/plan`。
- 一般、固定、重複單次及分割行程的移動都會同步；分割行程會重新計算整體起訖時間。
- 鎖定後自動排程不會把它移回原時間。
- 手動刪除 Bot 行程後，對應任務會標記為 `cancelled`。
- Calendar 增量同步在每小時的 `00` 與 `30` 分執行。

## 完成與未完成回饋

行程結束後，Bob 每 15 分鐘檢查一次並私訊回饋。按下最終評分時，Bob 會再讀取一次該 Calendar 行程的最新開始與結束時間；因此即使尚未等到半小時同步，ML 仍使用你手動移動後的實際時段。

完成任務使用兩階段評分：

1. 效率 1～5。
2. 精神狀況 1～5。

完成後 Calendar 變為綠色。選擇「未完成」時：

- 先要求確認，避免誤觸。
- 效率記為 0，精神預設為 1。
- 可選擇未完成原因，也可以略過。
- Calendar 變為紅色。
- 可選擇重新排程；Bob 會詢問剩餘時長並建立新任務。
- 原任務與 0 分歷史永久保留。

分段任務採逐段回饋：

- 每一段結束後各自詢問效率與精神，不等待最後一段。
- 每段分別保存 Calendar 最新時間、實際分鐘數及評分，ML 將每段視為一筆訓練資料。
- 完成的分段各自變綠；未完成的分段各自變紅。
- 某段未完成不會取消其他分段，並可只重新安排該段的剩餘工作。
- 全部完成時整體任務為 `completed`；完成與未完成混合時為 `partially_completed`。
- 舊版整體分段回饋繼續保留，不會轉換或刪除。

## ML 使用規則

- 舊版一次評分資料標記為 `legacy_overall`，保留但只作低權重參考。
- 新版分段回饋保存該段實際分鐘、實際開始時間、原任務總長、段落順序、總段數、前段休息與同一任務先前分段的累積分鐘。
- 模型會自動把名稱歸為記憶、閱讀、寫作、會議、靈修、運動、辦事、休閒或其他；累積至少 3 筆的常見完整名稱也會成為個人化特徵。
- 分別預測效率、精神與完成機率；同一長任務若短段的歷史表現明顯較好，允許分割時可優先選擇分段方案。
- 少於 20 筆新版有效回饋：使用保守預設，不讓 Random Forest 決定時段。
- 20～49 筆：漸進混合 ML 與預設分數。
- 50 筆以上：完整使用 ML 選擇較合適的可行時段。
- 截止時間、固定行程、優先度與作息限制高於 ML 分數。

## Slash Commands

- `/add task_name:`：輸入任務名稱後，用私人選單依序選擇截止日期、預估時間、截止時間、分割方式及優先度，不呼叫 Gemini。
- `/fixed title:`：輸入行程名稱後，用私人選單選擇日期、開始時間與時長；「建立固定行程」即為最終確認。
- `/repeat title:`：輸入系列名稱後，用兩頁私人選單設定星期、時間方式、時長、開始日、優先度、分割與到期方式。
- `/tasks query:`：查詢未完成任務；`query` 可留空。
- `/plan`：預設顯示未來 7 天，並以私人選單切換今天、3、7、14 或 30 天。
- `/delete`：從私人選單選擇等待排程或已排程的任務，再確認刪除；重複任務可選單次或整個系列。
- `/reset`：清除自己的全部任務、評分與 Bot Calendar 行程，必須再次確認。

### `/add` 選單操作

1. 輸入 `/add`，在 `task_name` 填任務名稱，例如「期末報告」。
2. 截止日期可選「沒有截止日期」、今天、明天、未來 21 天內日期或自訂日期。
3. 預估時間可直接選 15～240 分鐘（包含 180 分鐘），或輸入自訂分鐘數。
4. 有截止日期時，可使用當天作息結束時間，或選擇／輸入明確截止時間。
5. 分割可選不可分割、每段至少 15／30／45／60 分鐘或自訂分鐘數。
6. 選擇「一般」、「重要」或「緊急」；較高優先度會在空檔競爭時先被保留。
7. 按「建立任務」後才會寫入資料庫並嘗試排入 Calendar；按「取消」不會留下任務。


選擇的日期是「截止日期」，不是指定開始日；Bob 可以把任務排在現在到截止期限之間。若要固定在某天某時開始，改用 `/fixed` 或 `@Bot` 說明是固定行程。

### `/fixed` 選單操作

1. 輸入 `/fixed`，在 `title` 填行程名稱，例如「看醫生」。
2. 日期可選今天、明天、未來 21 天內日期或自訂日期。
3. 開始小時可選 00～23，分鐘以每 5 分鐘為一格。
4. 時長可選 15～240 分鐘（包含 180 分鐘），或自訂 5～720 分鐘。
5. 畫面會即時計算結束日期與時間；跨午夜時也會顯示隔日日期。
6. 按「建立固定行程」後才會檢查 Calendar 並寫入；若時段衝突，原選單會保留供修改。

固定行程建立後會鎖定，排程器不會自動移動，也不提供分割。


### `/repeat` 選單操作

1. 輸入 `/repeat`，在 `title` 填系列名稱，例如「閱讀」。
2. 第一頁複選發生星期；可全選每天、選連續範圍或任意不連續組合。
3. 選擇彈性排程或固定時間、每次時長、系列開始日期及優先度。
4. 固定時間的小時為 00～23，分鐘只有 00／15／30／45。
5. 彈性排程可設定不可分割，或為系列指定每段最短分鐘數。
6. 到期可選每 14 天詢問是否延長，或指定系列結束日期。
7. 按「建立重複系列」即為最終確認；也可返回第一頁修改或取消。

指定結束日期的系列仍只會滾動建立未來最多 14 天，之後每天自動補足，避免一次產生大量 Calendar 行程。固定時間若與 Calendar 衝突，回覆中會列出略過的次數；彈性系列則交由排程器在各日期作息內尋找空檔。

### `/plan` 選單操作

1. 輸入 `/plan`，不需要填寫參數；預設顯示未來 7 天。
2. 使用選單切換今天、未來 3／7／14／30 天。
3. 只顯示 Bob 已排入且尚未開始的任務，不列出手動建立的 Google Calendar 行程。
4. 結果依日期分組；固定行程顯示 `🔒`，分割任務逐段顯示 `(1/N)`。
5. 清單過長時使用「上一頁／下一頁」，不再只截取前 30 筆。

查詢 `/plan` 只讀取本機 SQLite，不會呼叫 Gemini 或 Google Calendar API。等待排程的任務仍使用 `/tasks` 查看。

### `/delete` 選單操作

1. 輸入 `/delete`，不需要填寫任務名稱或編號。
2. 從選單選擇一個等待排程或已排程任務；清單沿用 `/tasks` 顯示的未完成編號。
3. 核對名稱、類型、時間與時長後再按刪除；清單超過 25 筆時可切換上一頁或下一頁。
4. 一般、固定及分割任務按「確認刪除」；分割任務會刪除所有分段 Calendar 行程。
5. 重複任務可選「只刪除此行程」或「刪除整個系列」。整個系列只移除尚未完成的未來行程，歷史與評分保留。
6. `feedback_requested`、`completed`、`incomplete` 與已有評分歷史不會出現在選單中。

`/delete` 不呼叫 Gemini。它只在你最後按下紅色刪除按鈕後，才會修改 SQLite 與 Google Calendar。

## 部署後首次設定清單

新版部署成功後依序完成：

1. 確認 VM 時區為 `Asia/Taipei`。
2. 確認 Bot Log 出現 `Scheduler started` 與 `已登入 Discord`。
3. 告訴 Bob 現在是開學或未開學。
4. 設定開學平日與開學六日作息。
5. 設定未開學平日與未開學六日作息。
6. 使用 `@Bot 查看目前作息` 複核四組時間。
7. 確認 Discord 允許接收 Bob 私訊。
8. 新增一個 30 分鐘測試任務，確認 Calendar 寫入。
9. 在 Calendar 手動移動該任務，等待下一個 `00/30` 分同步並用 `/tasks` 複核。
10. 行程結束後測試效率、精神與未完成回饋。
11. 建立不設截止日的測試系列，確認卡片顯示 14 天後詢問延長。
12. 對一個重複行程執行刪除，確認會出現「只刪除此行程／刪除整個系列／取消」。

完整 Oracle 更新與回復流程請參閱 `ORACLE_DEPLOYMENT.md`。
